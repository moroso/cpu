         { r20 <- 0x80000000; r8 <- 0; r9 <- 7; r10 <- 0b110; }
         { *l(r20) <- r8; *l(r20 + 4) <- r9; r3 <- long; long handler; }
         { eha <- r3; *l(r20 + 8) <- r10; r11 <- 0b1; }
         { pflags <- r11; }
         { b sled; r1 <- 0; r2 <- 0 }
handler: { r0 <- 0x42; r3 <- r1; p1 <- r2 < 2; r2 <- r2 + 1; }
         { !p1 -> *l(r20 + 8) <- r10; }
         { eret; }
sled:    { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
         { r1 <- r1 + 1; }
end: